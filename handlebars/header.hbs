<header class="main-header">
  <div class="container">
    <h1 class="logo">{{logo}}</h1>
    <nav class="nav">
      <ul>
        {{#each menu}}
          <li><a href="{{href}}">{{text}}</a></li>
        {{/each}}
      </ul>
    </nav>
  </div>
</header>
